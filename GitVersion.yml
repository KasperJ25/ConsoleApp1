branches:
  develop:
    mode: ContinuousDeployment
    increment: Patch
    label: ''
  main:
    regex: ^main$
    increment: Minor
    source-branches: ['develop']
commit-message-incrementing: Enabled
